// DEPRECATED: Gallery component archived to `src/archived_unused/Gallery.tsx`.
// The original implementation was moved to the `archived_unused` folder to keep the project
// clean. This file is kept as a small placeholder to avoid accidental imports during development.

import React from 'react';

const Gallery: React.FC = () => null;

export default Gallery;